One-line: The clubpilot wordmark image (club green + pilot white) for dark backgrounds.

```jsx
<Wordmark height={56} />
<Wordmark src="../assets/img/wordmark-clubpilot.png" height={40} />
```

- Always on dark. Keep ≥40px clear-space; minimum width 120px. Pass `src` with the correct relative path for your page.
