One-line: The brand's signature three-bubble Triad mark (AI Assist · SMS · App) — use it whole as a corner mark, divider, or accent.

```jsx
<TriadMark size={120} />
<TriadMark size={64} tone="mono" color="#fff" />
```

- This is a faithful reproduction of the file's signature motif. Don't recolor or swap the glyphs (smile / two dots / ring). `tone="mono"` for a single-color outline version.
