One-line: The green, uppercase, tracked section label that opens almost every Club Pilot composition.

```jsx
<Eyebrow>Text Messaging</Eyebrow>
```

- Defaults to confirmation green `#0acb40`. Pass `color` to override (rarely).
