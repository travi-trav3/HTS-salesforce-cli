One-line: The oversized numeral treatment for stat / proof cards — the number is always the hero.

```jsx
<Stat value="98" unit="%" label="text open rate within 3 minutes" />
<Stat value="#2" label="Booth overall — PGA Show 2026" align="center" />
```

- `unit` renders in the accent color (confirmation green by default). Keep labels short.
